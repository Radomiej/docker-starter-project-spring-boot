version: '3'

services:
  docker-elasticsearch:
    image: "elasticsearch:latest"
    ports:
    - 0.0.0.0:9300:9300
  docker-redis:
    image: "redis:alpine"
  docker-mongodb:
    image: "mongo:latest"
    ports:
    - 0.0.0.0:27017:27017
  docker-spring-boot-rest:
    image: maven-springboot
    build: .
    depends_on:
    - docker-mongodb
    - docker-elasticsearch
    ports:
    - 8080:8080
    environment:
    - TEST=test